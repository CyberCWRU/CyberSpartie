def get_response(user_input: str) -> str:
    '''Gets the response to incoming messages based on some logic

    Args:
        user_input: the string content of the user message
    '''

    return "" # We aren't doing anything with this for now
